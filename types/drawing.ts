export interface Drawing {
    pageId: number
    paths: string[]
    colors: string[]
    lineWidths: number[]
  }
  
  export type DrawingResult = {
    paths: string[]
    colors: string[]
    lineWidths: number[]
  }
  
  